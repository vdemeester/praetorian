machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t vdemeester/praetorian .

test:
  override:
    - docker run -d -p 9200:9200 vdemeester/praetorian; sleep 1
